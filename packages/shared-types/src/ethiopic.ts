// Ethiopian Calendar (EC) display-layer stub (D-10, §40).
// Gregorian remains the operational calendar; real EC conversion (accounting
// for the ~7-8 year offset and Pagume 13th month) lands as a dedicated
// increment with the 200 golden test vectors called out in §50.

export function toEthiopianDisplay(isoDate: string): string {
  void isoDate;
  return "EC date — not yet implemented";
}
